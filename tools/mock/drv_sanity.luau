runMain(function()
	local p = newInstance("Part", WORKSPACE)
	print(pcall(function() return p.Foo end))
	print(pcall(function() p.Position = UDim2.new() end))
	print(pcall(function() p:SetAttribute("x", {}) end))
	print(pcall(function() Instance.new("Partt") end))
	print(pcall(function() return Enum.Material.Marbel end))
	task.spawn(function() local x = nil; return x.y end)
	local m = makeRig("x") m.Parent = WORKSPACE
	m:PivotTo(CFrame.new(10, 0, 0))
	print(m.HumanoidRootPart.Position, m:GetPivot().Position)
	local c = CFrame.lookAt(Vector3.new(0,0,0), Vector3.new(0,0,-10))
	print(c.LookVector, (CFrame.Angles(0, math.pi/2, 0)).LookVector)
	p:Destroy()
	print(pcall(function() p.Parent = WORKSPACE end))
end, 10)
report()
